---
name: jack-template
services:
  db:
    image: postgres
    container_name: jack-template-db
    restart: always
    environment:
      POSTGRES_PASSWORD: mysecretpassword
    volumes: [./init.sql:/docker-entrypoint-initdb.d/init.sql]
    ports: [5433:5432]
